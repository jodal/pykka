version: 2

build:
  image: latest

python:
  version: 3.7
  install:
    - method: pip
      path: .
    - method: pip
      path: toml

sphinx:
  builder: htmldir
  configuration: docs/conf.py

formats: all
